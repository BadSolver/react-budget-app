import styled from "styled-components";
import { StyledSearch } from "../Search/style";

export const StyledForm = styled.form`
    display: flex;
    flex-wrap:wrap ;
`
export const Input = styled(StyledSearch)`

`
